const { Message } = require('@projectriff/message');

module.exports = message => {
    // todo execution middleware chain

    // transform arguments

    // invoke HEROKU_FUNCTION_URI

    // transform response

    return 'wip';
};

module.exports.$argumentType = 'message';

Message.install();
